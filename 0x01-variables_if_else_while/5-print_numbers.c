#include<stdio.h>
/**
*main - Prints all single digits of base 10 followed by new line
*
*Return: returns 0
*/

int main(void)
{
int moa;
for (moa = 0; moa < 10; moa++)
{
printf("%d", moa);

}
printf("\n");
return (0);
}
